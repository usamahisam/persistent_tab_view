name: persistent_bottom_nav_bar
description: Includes up-to 20 styles.

# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 4.0.3

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
